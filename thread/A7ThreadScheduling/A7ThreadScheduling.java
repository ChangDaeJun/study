package thread.A7ThreadScheduling;

/**
 * [suspend()] & [resume()] & [stop()] - deprecated! 된 함수(데드락 발생 가능성을 높임)
 * 1. suspend(): 쓰레드를 멈춤.
 * 2. resume(): 멈춘 쓰레드를 실행.
 * 3. stop(): 쓰레드 즉시 종료.
 *
 * [yield()]
 * 자신에게 주어진 실행시간을 다음 쓰레드에게 양보.
 *
 * [join()]
 * 하던 작업을 잠시 멈추고, 지정 시간동안 다른 쓰레드가 작업 수행할 수 있게 해줌.
 */
public class A7ThreadScheduling {

}
